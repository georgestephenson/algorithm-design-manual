# Exercise 1.2

> *[3]* Show that $a \times b$ can be less than $min(a,b)$

This is true if one of $a$ and $b$ is smaller than $-1$ and the other is greater than $1$

For example,

$$
a = -2, b = 2 \\
a \times b = -4 \\
min(a,b) = -2 \\
a \times b < min(a,b)
$$

