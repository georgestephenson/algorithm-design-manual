# Exercise 1.1

> *[3]* Show that $a + b$ can be less than $min(a,b)$

This is true whenever $a < 0$ and $b < 0$.

For example,

$$
a = -1, b = -1 \\
a + b = -2 \\
min(a,b) = -1 \\
a + b < min(a,b)
$$

